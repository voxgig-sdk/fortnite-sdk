<?php
declare(strict_types=1);

// Fortnite SDK utility: prepare_path

class FortnitePreparePath
{
    public static function call(FortniteContext $ctx): string
    {
        $point = $ctx->point;
        $parts = [];
        if ($point) {
            $p = \Voxgig\Struct\Struct::getprop($point, 'parts');
            if (is_array($p)) {
                $parts = $p;
            }
        }
        return \Voxgig\Struct\Struct::join($parts, '/', true);
    }
}
